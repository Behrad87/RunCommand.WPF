<Page x:Class="RunCommand.WPF.Views.MultiServerQueryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:conv="clr-namespace:RunCommand.WPF.Converters"
      Title="Multi-Server Query" FlowDirection="LeftToRight">

    <Page.Resources>
        <conv:StatusToBrushConverter x:Key="StatusToBrush"/>
    </Page.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- toolbar -->
            <RowDefinition Height="2*"/>     <!-- server grid -->
            <RowDefinition Height="Auto"/>   <!-- query box + run -->
            <RowDefinition Height="Auto"/>   <!-- progress -->
            <RowDefinition Height="1.5*"/>   <!-- results -->
            <RowDefinition Height="Auto"/>   <!-- status bar -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8" LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Add Server..." Command="{Binding AddServerCommand}" Padding="10,4" Margin="4,0"/>
                <Button Content="Remove Selected" Command="{Binding RemoveSelectedCommand}" Padding="10,4" Margin="4,0"/>
                <Button Content="Check Selected" Command="{Binding CheckSelectedStatusCommand}" Padding="10,4" Margin="4,0"/>
                <Button Content="Check All" Command="{Binding CheckAllStatusCommand}" Padding="10,4" Margin="4,0"/>
            </StackPanel>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0" VerticalContentAlignment="Center" Padding="6,4">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </DockPanel>

        <!-- Server grid: virtualized, so it stays responsive with 1000+ rows -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ServersView}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Extended"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  ScrollViewer.CanContentScroll="True">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="36">
                    <DataGridTemplateColumn.Header>
                        <CheckBox Command="{Binding DataContext.ToggleSelectAllCommand, RelativeSource={RelativeSource AncestorType=Page}}"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="●" Width="30">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="12" Height="12"
                                     Fill="{Binding Status, Converter={StaticResource StatusToBrush}}"
                                     ToolTip="{Binding LastError}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="Host" Binding="{Binding HostName}" Width="140"/>
                <DataGridTextColumn Header="Database" Binding="{Binding DatabaseName}" Width="140"/>
                <DataGridTextColumn Header="Group" Binding="{Binding Group}" Width="100"/>
                <DataGridTextColumn Header="Last Checked" Binding="{Binding LastCheckedDisplay}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Response" Binding="{Binding ResponseTimeDisplay}" Width="90" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Query input -->
        <Grid Grid.Row="2" Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SqlText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="70"
                     FontFamily="Consolas" VerticalScrollBarVisibility="Auto"
                     Padding="6" Margin="0,0,8,0"/>
            <Button Grid.Column="1" Content="Run Query" Command="{Binding RunQueryCommand}"
                    Padding="16,4" VerticalAlignment="Bottom" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Content="Cancel" Command="{Binding CancelCommand}"
                    Padding="16,4" VerticalAlignment="Bottom"/>
        </Grid>

        <!-- Progress -->
        <Grid Grid.Row="3" Margin="0,0,0,8">
            <ProgressBar Minimum="0" Maximum="{Binding ProgressTotal}" Value="{Binding ProgressCurrent}" Height="16"/>
        </Grid>

        <!-- Results -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding Results}" AutoGenerateColumns="False"
                  CanUserAddRows="False" EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Server" Binding="{Binding ServerName}" Width="180"/>
                <DataGridTextColumn Header="Host" Binding="{Binding HostName}" Width="140"/>
                <DataGridTemplateColumn Header="OK" Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="12" Height="12">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#D93333"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Success}" Value="True">
                                                <Setter Property="Fill" Value="#2EA04A"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Rows" Binding="{Binding RowsAffectedOrReturned}" Width="70"/>
                <DataGridTextColumn Header="Duration (ms)" Binding="{Binding DurationMs}" Width="100"/>
                <DataGridTextColumn Header="Error" Binding="{Binding Error}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="5" Text="{Binding StatusMessage}" Margin="0,8,0,0" Opacity="0.8"/>
    </Grid>
</Page>
